from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Using cluster algorithm to segment customers based on purchase history',
    author='Nabeel Keblawi',
    license='MIT',
)
